const assert = require('assert');
const formatDate = require('./index');

describe('formatDate', () => {
  describe('seconds', () => {
    ...
  });

  describe('minutes', () => {
    ...
  });

  describe('hours', () => {
    ...
  });
});